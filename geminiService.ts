
// This file is a compatibility shim — the service layer has been migrated to
// OpenRouter (openrouterService.ts). Re-export everything from there.
export { OpenRouterHost as GeminiHost } from './openrouterService';
